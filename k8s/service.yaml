apiVersion: v1
kind: Service
metadata:
  name: brain-tasks-app-service
  labels:
    app: brain-tasks-app
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: brain-tasks-app
